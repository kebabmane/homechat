<%= render 'admin/header' %>

<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 mt-8 pb-24 space-y-8">
  <section class="rounded-xl border border-gray-200 bg-white shadow-sm">
    <header class="px-6 py-4 border-b border-gray-200">
      <h2 class="text-lg font-semibold text-gray-900">General</h2>
      <p class="text-sm text-gray-500">Control how HomeChat is presented to your team.</p>
    </header>
    <div class="px-6 py-6">
      <%= form_with url: admin_settings_path, method: :patch, local: true, class: "space-y-6" do %>
        <div class="space-y-4">
          <div>
            <label class="block text-sm font-medium text-gray-900">Site name</label>
            <input type="text" name="site_name" value="<%= @site_name %>" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500">
            <p class="mt-1 text-xs text-gray-500">Shown in window titles and the sidebar header.</p>
          </div>
          <div class="flex items-start gap-3">
            <input type="checkbox" id="allow_signups" name="allow_signups" value="1" <%= 'checked' if @allow_signups %> class="mt-1 h-4 w-4 rounded border-gray-300 text-blue-600 focus:ring-blue-500">
            <label for="allow_signups" class="text-sm text-gray-700 cursor-pointer">Allow new user sign-ups</label>
          </div>
        </div>

        <div class="rounded-xl border border-gray-100 bg-gray-50/80 px-4 py-5">
          <h3 class="text-sm font-semibold text-gray-900">Progressive Web App</h3>
          <p class="text-xs text-gray-500">Control installation prompts and manifest metadata.</p>
          <div class="mt-4 space-y-4">
            <div class="flex items-start gap-3">
              <input type="checkbox" id="pwa_enabled" name="pwa_enabled" value="1" <%= 'checked' if @pwa_enabled %> class="mt-1 h-4 w-4 rounded border-gray-300 text-blue-600 focus:ring-blue-500">
              <label for="pwa_enabled" class="text-sm text-gray-700 cursor-pointer">Enable installable PWA + offline cache</label>
            </div>
            <div class="grid gap-4 sm:grid-cols-2">
              <div>
                <label class="block text-sm font-medium text-gray-900">Short name</label>
                <input type="text" name="pwa_short_name" value="<%= @pwa_short_name %>" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500">
              </div>
              <div>
                <label class="block text-sm font-medium text-gray-900">Display mode</label>
                <select name="pwa_display" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500">
                  <option value="standalone" <%= 'selected' if @pwa_display == 'standalone' %>>Standalone (app-like)</option>
                  <option value="browser" <%= 'selected' if @pwa_display == 'browser' %>>Browser (tabs + URL bar)</option>
                </select>
              </div>
            </div>
            <div class="grid gap-4 sm:grid-cols-2">
              <div>
                <label class="block text-sm font-medium text-gray-900">Theme color</label>
                <input type="text" name="pwa_theme_color" value="<%= @pwa_theme_color %>" placeholder="#2563eb" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500">
              </div>
              <div>
                <label class="block text-sm font-medium text-gray-900">Background color</label>
                <input type="text" name="pwa_bg_color" value="<%= @pwa_bg_color %>" placeholder="#ffffff" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500">
              </div>
            </div>
            <p class="text-xs text-gray-500">Icons are served from <code class="font-mono">/icon.png</code> and <code class="font-mono">/icon.svg</code>.</p>
          </div>
        </div>

        <div class="rounded-xl border border-green-100 bg-green-50/70 px-4 py-5">
          <h3 class="text-sm font-semibold text-gray-900">API & Integrations</h3>
          <p class="text-xs text-gray-600">Control external access to HomeChat via the REST API.</p>
          <div class="mt-4 space-y-4">
            <div class="flex items-start gap-3">
              <input type="checkbox" id="api_enabled" name="api_enabled" value="1" <%= 'checked' if @api_enabled %> class="mt-1 h-4 w-4 rounded border-gray-300 text-green-600 focus:ring-green-500">
              <label for="api_enabled" class="text-sm text-gray-700 cursor-pointer">Enable REST API for external integrations</label>
            </div>
            <div class="flex items-start gap-3">
              <input type="checkbox" id="home_assistant_enabled" name="home_assistant_enabled" value="1" <%= 'checked' if @home_assistant_enabled %> class="mt-1 h-4 w-4 rounded border-gray-300 text-green-600 focus:ring-green-500">
              <label for="home_assistant_enabled" class="text-sm text-gray-700 cursor-pointer">Enable Home Assistant integration</label>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-900">Webhook base URL</label>
              <input type="text" name="webhook_base_url" value="<%= @webhook_base_url %>" placeholder="<%= request.base_url %>" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-green-500 focus:ring-green-500">
              <p class="mt-1 text-xs text-gray-500">Base URL for webhook callbacks. Auto-detected if left blank.</p>
            </div>
          </div>
        </div>

        <div class="rounded-xl border border-orange-100 bg-orange-50/70 px-4 py-5">
          <h3 class="text-sm font-semibold text-gray-900">Push Notifications (FCM)</h3>
          <p class="text-xs text-gray-600">Enable Firebase Cloud Messaging for mobile push notifications.</p>
          <div class="mt-4 space-y-4">
            <div class="flex items-start gap-3">
              <input type="checkbox" id="fcm_enabled" name="fcm_enabled" value="1" <%= 'checked' if @fcm_enabled %> class="mt-1 h-4 w-4 rounded border-gray-300 text-orange-600 focus:ring-orange-500">
              <div>
                <label for="fcm_enabled" class="text-sm text-gray-700 cursor-pointer">Enable push notifications</label>
                <% if @fcm_configured %>
                  <span class="ml-2 inline-flex items-center rounded-full bg-green-100 px-2 py-0.5 text-xs font-medium text-green-700">Configured</span>
                <% end %>
              </div>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-900">Firebase Project ID</label>
              <input type="text" name="fcm_project_id" value="<%= @fcm_project_id %>" placeholder="my-homechat-project" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-orange-500 focus:ring-orange-500">
              <p class="mt-1 text-xs text-gray-500">Found in your Firebase Console project settings.</p>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-900">Service Account Client Email</label>
              <input type="text" name="fcm_client_email" value="<%= @fcm_client_email %>" placeholder="firebase-adminsdk-xxxxx@my-project.iam.gserviceaccount.com" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-orange-500 focus:ring-orange-500">
              <p class="mt-1 text-xs text-gray-500">The client_email from your Firebase service account JSON.</p>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-900">Service Account Private Key</label>
              <textarea name="fcm_private_key" rows="3" placeholder="-----BEGIN PRIVATE KEY-----
...
-----END PRIVATE KEY-----" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-orange-500 focus:ring-orange-500 font-mono text-xs"></textarea>
              <% if @fcm_private_key_present %>
                <p class="mt-1 text-xs text-gray-500">A key is already stored. Leave blank to keep it.</p>
                <div class="mt-1 inline-flex items-center gap-2 text-xs text-gray-600">
                  <input type="checkbox" id="reset_fcm_private_key" name="reset_fcm_private_key" value="1" class="h-4 w-4 rounded border-gray-300 text-orange-600 focus:ring-orange-500">
                  <label for="reset_fcm_private_key" class="cursor-pointer">Reset stored key</label>
                </div>
              <% else %>
                <p class="mt-1 text-xs text-gray-500">The private_key from your Firebase service account JSON.</p>
              <% end %>
            </div>
            <p class="text-xs text-gray-500">Download your service account JSON from Firebase Console > Project Settings > Service Accounts > Generate new private key.</p>
          </div>
        </div>

        <div class="rounded-xl border border-purple-100 bg-purple-50/70 px-4 py-5">
          <h3 class="text-sm font-semibold text-gray-900">AI & LiteLLM</h3>
          <p class="text-xs text-gray-600">Proxy chat completions through your LiteLLM instance and pick the default model for server-managed bots.</p>
          <div class="mt-4 space-y-4">
            <div>
              <label class="block text-sm font-medium text-gray-900">LiteLLM host</label>
              <input type="text" name="litellm_host" value="<%= @litellm_host %>" placeholder="http://127.0.0.1:4000" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-purple-500 focus:ring-purple-500">
              <p class="mt-1 text-xs text-gray-500">HomeChat will call <code class="font-mono">/v1/models</code> and <code class="font-mono">/v1/chat/completions</code> on this base URL.</p>
              <% if @litellm_error.present? %>
                <p class="mt-1 text-xs text-rose-600"><%= @litellm_error %></p>
              <% end %>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-900">LiteLLM API key</label>
              <input type="password" name="litellm_api_key" value="" placeholder="sk-live-..." autocomplete="off" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-purple-500 focus:ring-purple-500">
              <% if @litellm_api_key_present %>
                <p class="mt-1 text-xs text-gray-500">A key is already stored. Leave blank to keep it.</p>
                <div class="mt-1 inline-flex items-center gap-2 text-xs text-gray-600">
                  <input type="checkbox" id="reset_litellm_api_key" name="reset_litellm_api_key" value="1" class="h-4 w-4 rounded border-gray-300 text-purple-600 focus:ring-purple-500">
                  <label for="reset_litellm_api_key" class="cursor-pointer">Reset stored key</label>
                </div>
              <% else %>
                <p class="mt-1 text-xs text-gray-500">Paste the API key your LiteLLM instance expects. Required for authentication.</p>
              <% end %>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-900">Default model</label>
              <% if @litellm_models.present? %>
                <select name="litellm_default_model" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-purple-500 focus:ring-purple-500">
                  <option value="">Select a model</option>
                  <% @litellm_models.each do |model| %>
                    <option value="<%= model %>" <%= 'selected' if model == @litellm_default_model %>><%= model %></option>
                  <% end %>
                </select>
                <p class="mt-1 text-xs text-gray-500">Used when AI bots do not override their model.</p>
              <% else %>
                <input type="text" name="litellm_default_model" value="<%= @litellm_default_model %>" placeholder="gpt-4o-mini" class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-purple-500 focus:ring-purple-500">
                <p class="mt-1 text-xs text-gray-500">Enter a model id manually or configure the host above to fetch available models.</p>
              <% end %>
            </div>
          </div>
        </div>
        <div class="flex justify-end">
          <button type="submit" class="inline-flex items-center rounded-md bg-blue-600 px-4 py-2 text-sm font-medium text-white transition hover:bg-blue-700 focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-500">Save settings</button>
        </div>
      <% end %>
    </div>
  </section>
</div>
