<% content_for :no_top_padding, true %>
<div class="h-full p-6 overflow-y-auto bg-gray-50">
  <div class="max-w-4xl mx-auto">
    <div class="mb-8">
      <div class="flex items-center justify-between mb-6">
        <div>
          <h1 class="text-3xl font-bold text-gray-900 mb-2">Browse All Channels</h1>
          <p class="text-gray-600">Discover and join conversations across the community</p>
        </div>
        <%= link_to "Create New Channel", new_channel_path,
            class: "inline-flex items-center px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white text-sm font-medium rounded-lg transition-colors" %>
      </div>
      <div class="max-w-md">
        <label for="channel-search" class="sr-only">Search channels</label>
        <input id="channel-search" name="q" type="search" placeholder="Search channels..." value="<%= params[:q] %>"
               aria-label="Search channels"
               class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"
               data-controller="channel-search <%= 'focus' if params[:focus] == 'search' %>"
               data-channel-search-target="input"
               data-action="input->channel-search#filter">
      </div>
    </div>

    <!-- All Public Channels -->
    <div class="bg-white rounded-lg border border-gray-200 mb-8">
      <div class="p-6 border-b border-gray-200">
        <div class="flex items-center justify-between">
          <div>
            <h2 class="text-xl font-semibold text-gray-900">Public Channels</h2>
            <p class="text-sm text-gray-600 mt-1">Open to everyone - <%= @public_channels.count %> channel<%= "s" unless @public_channels.count == 1 %></p>
          </div>
        </div>
      </div>

      <div class="p-6">
        <% if @public_channels.any? %>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6" data-channel-search-target="items">
            <% @public_channels.each do |channel| %>
              <div class="hover-lift border border-gray-200 rounded-lg p-5 hover:border-blue-200 bg-white" data-channel-search-target="item" data-name="<%= channel.name %>">
                <div class="flex items-start justify-between">
                  <div class="flex-1 min-w-0">
                    <div class="flex items-center space-x-2 mb-3">
                      <span class="text-gray-400 text-lg">#</span>
                      <h3 class="text-lg font-semibold text-gray-900 truncate">
                        <%= link_to channel.name, channel_path(channel), class: "hover:text-blue-600 transition-colors" %>
                      </h3>
                    </div>
                    <% if channel.description.present? %>
                      <p class="text-sm text-gray-600 mb-3 line-clamp-2"><%= channel.description %></p>
                    <% end %>
                    <div class="flex items-center space-x-4 text-xs text-gray-500 mb-4">
                      <div class="flex items-center space-x-1">
                        <svg class="w-3 h-3" fill="currentColor" viewBox="0 0 20 20">
                          <path fill-rule="evenodd" d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z" clip-rule="evenodd"></path>
                        </svg>
                        <span>Created by <%= channel.creator.username %></span>
                      </div>
                      <div class="flex items-center space-x-1">
                        <svg class="w-3 h-3" fill="currentColor" viewBox="0 0 20 20">
                          <path d="M9 6a3 3 0 11-6 0 3 3 0 016 0zM17 6a3 3 0 11-6 0 3 3 0 016 0zM12.93 17c.046-.327.07-.66.07-1a6.97 6.97 0 00-1.5-4.33A5 5 0 0119 16v1h-6.07zM6 11a5 5 0 015 5v1H1v-1a5 5 0 015-5z"></path>
                        </svg>
                        <span><%= channel.member_count %> member<%= "s" if channel.member_count != 1 %></span>
                      </div>
                    </div>
                  </div>
                </div>
                <div class="flex items-center justify-between">
                  <div class="text-xs text-gray-400">
                    Created <%= time_ago_in_words(channel.created_at) %> ago
                  </div>
                  <div class="flex-shrink-0">
                    <% if current_user.member_of?(channel) %>
                      <%= link_to "Enter", channel_path(channel), class: "px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white text-sm font-medium rounded-md transition-colors" %>
                    <% else %>
                      <%= link_to "Join", join_channel_path(channel), method: :post, class: "px-4 py-2 bg-green-600 hover:bg-green-700 text-white text-sm font-medium rounded-md transition-colors" %>
                    <% end %>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        <% else %>
          <div class="text-center py-12">
            <div class="w-24 h-24 mx-auto mb-6" aria-hidden="true">
              <!-- Channels/rooms illustration -->
              <svg viewBox="0 0 120 120" fill="none" xmlns="http://www.w3.org/2000/svg" class="w-full h-full">
                <circle cx="60" cy="60" r="56" fill="#F0FDF4" stroke="#BBF7D0" stroke-width="2"/>
                <!-- Door frame -->
                <rect x="40" y="30" width="40" height="55" rx="4" fill="#22C55E" opacity="0.2"/>
                <rect x="45" y="35" width="30" height="45" rx="2" fill="white" stroke="#22C55E" stroke-width="2"/>
                <!-- Door handle -->
                <circle cx="70" cy="57" r="3" fill="#22C55E"/>
                <!-- Plus sign above door -->
                <rect x="56" y="18" width="8" height="2" rx="1" fill="#22C55E"/>
                <rect x="59" y="15" width="2" height="8" rx="1" fill="#22C55E"/>
                <!-- Welcome mat -->
                <rect x="42" y="82" width="36" height="6" rx="2" fill="#86EFAC"/>
                <!-- Sparkles -->
                <circle cx="28" cy="45" r="3" fill="#FCD34D"/>
                <circle cx="95" cy="35" r="2" fill="#A5B4FC"/>
              </svg>
            </div>
            <h3 class="text-xl font-medium text-gray-900 mb-3">No public channels yet</h3>
            <p class="text-gray-500 mb-6 max-w-sm mx-auto">Be the first to create a public channel for everyone to join!</p>
            <%= link_to "Create First Channel", new_channel_path, class: "btn-press inline-flex items-center px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white font-medium rounded-lg transition-colors" %>
          </div>
        <% end %>
      </div>
    </div>

    <!-- My Channels -->
    <div class="bg-white rounded-lg border border-gray-200">
      <div class="p-6 border-b border-gray-200">
        <div class="flex items-center justify-between">
          <div>
            <h2 class="text-xl font-semibold text-gray-900">My Channels</h2>
            <p class="text-sm text-gray-600 mt-1">Channels you've joined - <%= @my_channels.count %> channel<%= "s" unless @my_channels.count == 1 %></p>
          </div>
        </div>
      </div>

      <div class="p-6">
        <% if @my_channels.any? %>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <% @my_channels.each do |channel| %>
              <div class="hover-lift border border-gray-200 rounded-lg p-5 hover:border-blue-200 bg-white">
                <div class="flex items-start justify-between">
                  <div class="flex-1 min-w-0">
                    <div class="flex items-center space-x-2 mb-3">
                      <span class="text-gray-400 text-lg">#</span>
                      <h3 class="text-lg font-semibold text-gray-900 truncate">
                        <%= link_to channel.name, channel_path(channel), class: "hover:text-blue-600 transition-colors" %>
                      </h3>
                      <% if channel.private? %>
                        <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">
                          <svg class="w-3 h-3 mr-1" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M5 9V7a5 5 0 0110 0v2a2 2 0 012 2v5a2 2 0 01-2 2H5a2 2 0 01-2-2v-5a2 2 0 012-2zm8-2v2H7V7a3 3 0 016 0z" clip-rule="evenodd"></path>
                          </svg>
                          Private
                        </span>
                      <% end %>
                    </div>
                    <% if channel.description.present? %>
                      <p class="text-sm text-gray-600 mb-3 line-clamp-2"><%= channel.description %></p>
                    <% end %>
                    <div class="flex items-center space-x-4 text-xs text-gray-500 mb-4">
                      <div class="flex items-center space-x-1">
                        <svg class="w-3 h-3" fill="currentColor" viewBox="0 0 20 20">
                          <path fill-rule="evenodd" d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z" clip-rule="evenodd"></path>
                        </svg>
                        <span>
                          <% if channel.creator == current_user %>
                            Created by you
                          <% else %>
                            Created by <%= channel.creator.username %>
                          <% end %>
                        </span>
                      </div>
                      <div class="flex items-center space-x-1">
                        <svg class="w-3 h-3" fill="currentColor" viewBox="0 0 20 20">
                          <path d="M9 6a3 3 0 11-6 0 3 3 0 016 0zM17 6a3 3 0 11-6 0 3 3 0 016 0zM12.93 17c.046-.327.07-.66.07-1a6.97 6.97 0 00-1.5-4.33A5 5 0 0119 16v1h-6.07zM6 11a5 5 0 015 5v1H1v-1a5 5 0 015-5z"></path>
                        </svg>
                        <span><%= channel.member_count %> member<%= "s" if channel.member_count != 1 %></span>
                      </div>
                    </div>
                  </div>
                </div>
                <div class="flex items-center justify-between">
                  <div class="text-xs text-gray-400">
                    Joined <%= time_ago_in_words(current_user.channel_memberships.find_by(channel: channel)&.joined_at || channel.created_at) %> ago
                  </div>
                  <div class="flex items-center space-x-2">
                    <%= link_to "Enter", channel_path(channel), class: "px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white text-sm font-medium rounded-md transition-colors" %>
                    <%= link_to leave_channel_path(channel),
                        data: {
                          turbo_method: :delete,
                          turbo_confirm: "Are you sure you want to leave this channel?"
                        },
                        class: "text-gray-400 hover:text-red-600 transition-colors p-2",
                        title: "Leave channel" do %>
                      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
                      </svg>
                    <% end %>
                  </div>
                </div>
              </div>
            <% end %>
          </div>
        <% else %>
          <div class="text-center py-12">
            <div class="w-24 h-24 mx-auto mb-6" aria-hidden="true">
              <!-- Compass/explore illustration -->
              <svg viewBox="0 0 120 120" fill="none" xmlns="http://www.w3.org/2000/svg" class="w-full h-full">
                <circle cx="60" cy="60" r="56" fill="#FEF3C7" stroke="#FDE68A" stroke-width="2"/>
                <!-- Compass circle -->
                <circle cx="60" cy="60" r="32" fill="white" stroke="#F59E0B" stroke-width="2"/>
                <!-- Compass needle -->
                <path d="M60 35L65 60H55L60 35Z" fill="#EF4444"/>
                <path d="M60 85L55 60H65L60 85Z" fill="#3B82F6"/>
                <!-- Direction markers -->
                <circle cx="60" cy="32" r="2" fill="#F59E0B"/>
                <circle cx="60" cy="88" r="2" fill="#F59E0B"/>
                <circle cx="32" cy="60" r="2" fill="#F59E0B"/>
                <circle cx="88" cy="60" r="2" fill="#F59E0B"/>
                <!-- Center dot -->
                <circle cx="60" cy="60" r="4" fill="#F59E0B"/>
                <!-- Sparkles -->
                <circle cx="25" cy="30" r="3" fill="#A5B4FC"/>
                <circle cx="95" cy="85" r="2.5" fill="#86EFAC"/>
              </svg>
            </div>
            <h3 class="text-xl font-medium text-gray-900 mb-3">Ready to explore?</h3>
            <p class="text-gray-500 mb-6 max-w-sm mx-auto">Join a public channel or create your own to start chatting!</p>
            <div class="flex flex-col sm:flex-row items-center justify-center gap-3">
              <%= link_to "Create Channel", new_channel_path, class: "btn-press inline-flex items-center px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white text-sm font-medium rounded-lg transition-colors" %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
