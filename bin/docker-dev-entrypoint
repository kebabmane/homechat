#!/bin/bash -e

# Ensure gems are installed (for volume-mounted bundle cache)
bundle check || bundle install --jobs="${BUNDLE_JOBS:-4}" --retry="${BUNDLE_RETRY:-3}"

# Prepare database
./bin/rails db:prepare

# Execute the command
exec "$@"
